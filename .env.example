# Add these in Vercel Project Settings > Environment Variables for Production,
# Preview, and Development. SESSION_SECRET must be the same strong random value
# in every environment that needs to validate sessions created by another deployment.
# Keep the values out of source control.

# Required: generate a unique 32+ byte random value; never use a public URL or default.
SESSION_SECRET=
NEXT_PUBLIC_BASE_URL=
NEXT_PUBLIC_SUPABASE_URL=https://tlbhwvvzdiisjaagvcmt.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
CASHFREE_ENVIRONMENT=production
CASHFREE_CLIENT_ID=
CASHFREE_CLIENT_SECRET=

# Set NEXT_PUBLIC_BASE_URL to https://subhanacademy.in for Production (and each deployment's own URL elsewhere).
# Cashfree webhook URL: <NEXT_PUBLIC_BASE_URL>/api/webhooks/cashfree
